@page "{tagName}"
@model ChatBeet.Pages.Tags.TagModel
@{
    ViewData["Title"] = $"Stats for {Model.TagName}";
}

<h1>Stats for <span class="text-secondary">@Model.TagName.Replace("_", " ")</span></h1>


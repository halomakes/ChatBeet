namespace NetIRC
{
    public enum IRCCommand
    {
        UNKNOWN,
        NICK,
        MODE,
        QUIT,
        JOIN,
        PART,
        TOPIC,
        INVITE,
        KICK,
        PRIVMSG,
        NOTICE,
        PING,
        ERROR
    }
}
